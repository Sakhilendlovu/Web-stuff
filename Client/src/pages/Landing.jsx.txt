import { Link } from 'react-router-dom';

export default function Landing() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-teal-500 to-yellow-400 text-white p-8">
      <header className="flex justify-between items-center">
        <h1 className="text-3xl font-bold">ThutoAI</h1>
        <div>
          <Link to="/login" className="mr-4">Login</Link>
          <Link to="/register" className="bg-white text-teal-600 px-4 py-2 rounded">Sign Up</Link>
        </div>
      </header>
      <main className="mt-20 text-center">
        <h2 className="text-4xl font-semibold">Smart Tutoring. Local Language. Real Results.</h2>
        <p className="mt-4 text-lg">Get help with Math, Science, and Coding—aligned to South African curriculum.</p>
        <Link to="/dashboard" className="mt-6 inline-block bg-white text-teal-600 px-6 py-3 rounded">Start Learning</Link>
      </main>
    </div>
  );
}
